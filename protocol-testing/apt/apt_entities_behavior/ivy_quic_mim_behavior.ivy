#lang ivy1.7

include order
include quic_infer
include file
include ivy_quic_shim_mim
include quic_random_value

after init {
    sock_mim_server := quic_net.listen(endpoint_id.man_in_the_middle,server.ep);
    mim_agent.set_tls_id(4);
}
