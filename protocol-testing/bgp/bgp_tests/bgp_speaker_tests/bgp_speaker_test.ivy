#lang ivy1.7

include order
include bgp_file
include bgp_shim
include ivy_bgp_speaker_behavior

relation slept_bgp

after init {
    sock      := net.open(endpoint_id.speaker,client.ep);
    slept_bgp := false;
}

export frame.pong.handle
export frame.timestamp.handle
export bgp_packet_event
export speaker_send_event

export action _finalize = {
    
}